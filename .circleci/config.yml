version: 2.1

orbs:
  sonarcloud: sonarsource/sonarcloud@1.3

workflows:
  build_and_test:
    jobs:
      - build  # Your other job for building the project
      - sonarcloud/scan:
          context: sonarcloud
          requires:
            - build  # Ensures SonarCloud scan runs after build job completes

